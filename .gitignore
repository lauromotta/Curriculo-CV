# PDFs de referência da Catho e artefatos temporários (não fazem parte do repo)
*.pdf

# Exceção: o PDF do currículo final é entregue no repo
!Curriculo-Lauro-Motta.pdf

# Arquivos temporários de renderização
page_*.png
v2_page_*.png

# Sistema operacional
Thumbs.db
.DS_Store

# Node (dependências instaladas via npm install no CI/local)
node_modules/

# Backups e lixo gerado por ferramentas
*.bak
*.orig

# Mockups de design (mesa de rascunho — não são produto)
mockups/